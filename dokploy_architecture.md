# Архитектура и развертывание проекта Grand Transfer

В этом документе описана инфраструктура проекта после переезда с Vercel на собственный VPS (Virtual Private Server), а также инструкция по обновлению сайта.

## 🚀 Как заливать изменения на сайт

Процесс обновления полностью автоматизирован благодаря интеграции Dokploy и GitHub (настроен триггер On Push).

**Алгоритм действий для обновления:**
1. Внесите изменения в код у себя на компьютере в редакторе (VS Code).
2. Сохраните файлы.
3. Сделайте коммит и отправьте изменения (push) в ветку `main` на GitHub (например, через меню Source Control в VS Code или командой `git push`).
4. **Готово!** Dokploy автоматически "увидит" изменения, скачает новый код, соберет проект (`npm run build`) и обновит сайт без простоев.

*Примечание:* Если вы меняли переменные окружения (.env) или по какой-то причине автоматическая сборка не стартовала, вы можете запустить её вручную:
- Зайдите в панель Dokploy (http://155.212.216.227:3000)
- Откройте проект `grand-transfer` -> сервис `main` -> вкладка **Deployments**
- Нажмите кнопку **Deploy**.

---

## 🏗️ Как работает новая инфраструктура (Точное описание)

Чтобы сайт открылся у клиента по защищенному адресу `https://межгород.com`, запрос проходит через несколько независимых систем. 

### 1. REG.RU (Регистратор домена)
- **Роль:** Владелец доменного имени.
- **Что делает:** Хранит настройки DNS (Domain Name System). Когда пользователь вводит `межгород.com`, глобальная система доменных имен обращается к серверам REG.RU, чтобы узнать, на каком сервере физически находится сайт.
- **Настройка:** В REG.RU прописана **A-запись**, которая перенаправляет трафик по домену `межгород.com` на IP-адрес вашего сервера (`155.212.216.227`).

### 2. Beget (VPS Сервер)
- **Роль:** Физическая/виртуальная машина (железо), где работает сайт 24/7.
- **Что делает:** Это ваш собственный компьютер в дата-центре (с процессором, памятью и диском на базе Linux/Ubuntu). Он принимает все входящие соединения по IP-адресу. Это полная замена тарифным планам Vercel — теперь вы ни от кого не зависите и платите только за сам сервер.

### 3. Dokploy (Панель управления)
- **Роль:** Личный "Vercel", автоматизатор процессов разработки (PaaS - Platform as a Service).
- **Что делает:** Установлен поверх сервера Beget. Обычный сервер не умеет сам скачивать код, собирать Next.js приложения и выписывать SSL-сертификаты. Dokploy берет эту работу на себя:
  - Интегрируется с GitHub.
  - Изолирует приложение в Docker-контейнер (Nixpacks), обеспечивая стабильность (используя нужную версию Node.js, в нашем случае 20).
  - Включает в себя веб-сервер (встроенный Traefik роутер), который ловит запросы к `межгород.com` на сервере Beget и направляет их внутрь контейнера с вашим сайтом.
  - Автоматически получает и продлевает бесплатные сертификаты Let's Encrypt (HTTPS).

### 4. GitHub (Хранилище кода)
- **Роль:** Облачный репозиторий.
- **Что делает:** Хранит всю историю изменений исходного кода проекта. Является связующим звеном между локальным компьютером разработчика и сервером Dokploy. При отправке нового кода на GitHub, он генерирует событие Webhook, которое дает команду Dokploy начать новую сборку.

---

### 📝 Путь запроса (от пользователя к сайту)

1. Клиент вводит `https://межгород.com` в браузере.
2. Браузер спрашивает у DNS Рег.ру: *"Какой IP у этого сайта?"*.
3. Получает ответ: `155.212.216.227`.
4. Запрос летит на сервер **Beget** (`155.212.216.227`).
5. На сервере Beget этот запрос перехватывает маршрутизатор **Dokploy**.
6. Dokploy расшифровывает HTTPS трафик (используя полученный сертификат) и направляет запрос внутрь Docker-контейнера.
7. Внутри контейнера работает ваше приложение **Next.js** (код с GitHub на Node.js 20).
8. Next.js генерирует страницу сайта и отдает её клиенту обратно по той же цепочке.
