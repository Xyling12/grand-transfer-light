# GrandTransfer — Междугородний Трансфер

Премиальный сервис междугородних трансферов. Сайт позволяет клиентам точно рассчитать стоимость поездки между городами, выбрать класс автомобиля и оформить заявку. Заказы автоматически маршрутизируются в Telegram-бота для одобренных водителей.

---

## 🌟 Основной функционал

### 1. Умный калькулятор маршрутов
*   **Карты и геокодинг:** Интеграция с `Leaflet` и API `Nominatim (OpenStreetMap)` для живого поиска адресов и городов (с поддержкой РФ, РБ, КЗ и новых территорий).
*   **Маршрутизация OSRM:** Автоматическое построение линии маршрута на интерактивной карте и высчитывание точного километража и времени в пути.
*   **Динамические тарифы:** Стоимость километра зависит от выбранного класса авто (Эконом, Стандарт, Комфорт, Комфорт+, Бизнес, Минивэн) и города отправления/назначения.
*   **Секретные КПП:** Для приграничных зон (например, Успенка) калькулятор умеет разбивать маршрут на 2 участка ("До границы" и "После границы") с перерасчетом стоимости по разным тарифам.

### 2. Закрытая система водителей (Telegram Bot)
*   **Модель по подписке (Фундамент):** Бот (`@grand_transfer_bot`) работает не для всех. Любой новый водитель, нажавший `/start`, получает статус **PENDING** (В ожидании).
*   **Роутинг заказов:** Скрипт сайта (`src/lib/telegram.ts`) рассылает уведомления о новых заказах **только** водителям со статусом **APPROVED**.
*   **Меню команд:** Интерактивное Reply-меню внизу чата для проверки статистики сервиса.

### 3. Секретная Панель Администратора (Admin Panel)
*   Доступна по адресу: `/admin/drivers`
*   Защищена хардкоженым PIN-кодом (по умолчанию: `7878`).
*   Позволяет владельцу сайта:
    *   Видеть всех водителей, кто запустил бота (Telegram ID, Имя, Username).
    *   Выдавать доступы (`Одобрить` -> APPROVED).
    *   Блокировать или удалять провинившихся водителей (`Бан` -> BANNED).

---

## 🛠 Технологический стек
*   **Фреймворк:** Next.js (React) + TypeScript
*   **Стилизация:** Чистый CSS с использованием модулей (`module.css`) и CSS-переменных.
*   **База данных:** Prisma ORM + локальный бесплатный `SQLite` (файл базы лежит автономно).
*   **Боты и API:** `Telegraf` (для логики Telegram-бота).
*   **Хостинг:** Сайт развернут на Vercel.

---

## 🚀 Как запустить проект локально (Для себя)

```bash
# 1. Установить все зависимости проекта
npm install

# 2. Обновить структуру базы данных (если были изменения в schema.prisma)
npx prisma db push

# 3. Запустить сайт в режиме разработчика (будет доступен по адресу http://localhost:3000)
npm run dev
```

В отдельном окне терминала (чтобы бот реагировал на команды `/start` и `Статистика`):
```bash
# 4. Запустить Telegram Бота в фоновом режиме
npx tsx scripts/bot.ts
```

---

## 📝 Ключевые файлы для редактирования

*   `src/data/tariffs.ts` — Здесь лежат **базовые цены и тарифные сетки** (сколько стоит 1 км для каждого города и класса авто). Чтобы изменить цену за километр, редактируйте этот файл.
*   `src/data/cities.ts` — Справочник городов и КПП (граница).
*   `src/components/BookingForm.tsx` — Вся математика калькулятора (расчеты, карты, форма заявки).
*   `.env` — Настройки (Токен от Telegram Бота и Chat ID владельца).
*   `gemini.md` — Бортовой журнал изменений от ИИ-помощника. Здесь записана история всех глубоких настроек и структура проекта.
