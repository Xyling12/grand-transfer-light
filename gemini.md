# 🚀 Grand Transfer - Статус проекта (Февраль 2026)

Этот файл создан для того, чтобы быстро вспомнить контекст и продолжить работу на другом компьютере.

## 📦 Архитектура и Деплой
Сайт и бот развернуты на VPS (Beget) с помощью **Dokploy**.

**Ключевая особенность:** Приложение разделено на 2 отдельных контейнера (`web` и `bot`), чтобы избежать конфликтов портов и зависаний.

### Конфигурация Dokploy (Вкладка Raw)
Поскольку в Dokploy используется ручной ввод Compose (`Raw`), **изменения в файле `docker-compose.yml` на GitHub не применяются там автоматически!** 
Если нужно пересоздать приложение, в окно Compose в Dokploy нужно вставлять этот код:

```yaml
version: '3.8'
services:
  web:
    build:
      context: https://github.com/Xyling12/grand-transfer.git#main
      dockerfile: Dockerfile
    restart: unless-stopped
    networks:
      - dokploy-network
    volumes:
      - app_data:/app/prisma
    environment:
      - DATABASE_URL=file:/app/prisma/dev.db
      - TELEGRAM_BOT_TOKEN=8410817305:AAE6vTlmSuBBWA2Wbwf9_p5wy3QzqF6INz8
      - TELEGRAM_CHAT_ID=376060133
      - EMAIL_PASS=wororvoeoxunilkg
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.site-router.rule=Host(`xn--c1acbe2apap.com`) || Host(`www.xn--c1acbe2apap.com`)"
      - "traefik.http.services.site-router.loadbalancer.server.port=3000"
      - "traefik.docker.network=dokploy-network"

  bot:
    build:
      context: https://github.com/Xyling12/grand-transfer.git#main
      dockerfile: Dockerfile
    command: sh -c "chmod +x wait-for-db.sh && ./wait-for-db.sh"
    restart: unless-stopped
    networks:
      - dokploy-network
    volumes:
      - app_data:/app/prisma
    environment:
      - DATABASE_URL=file:/app/prisma/dev.db
      - TELEGRAM_BOT_TOKEN=8410817305:AAE6vTlmSuBBWA2Wbwf9_p5wy3QzqF6INz8
      - TELEGRAM_CHAT_ID=376060133
      - EMAIL_PASS=wororvoeoxunilkg

networks:
  dokploy-network:
    external: true

volumes:
  app_data:
```

---

## 🛠️ Что было исправлено (Последние работы)

### 1. Доработка функционала Диспетчеров (1.3.0)
**Суть:** Полноценный workflow для обработки заявок диспетчерами до их публикации в общую ленту водителей.
**Реализация:**
- Уведомления о новых заявках теперь содержат кнопку `🎧 Взять в работу`.
- Когда диспетчер забирает заявку, статус меняется на `PROCESSING` (Фиолетовый `🟣`), а **уведомление автоматически удаляется у всех остальных администраторов и диспетчеров**.
- Взявший диспетчер получает кнопку `📋 Полная заявка` для просмотра имени и телефона клиента. 
- Команда `👀 Активные заявки` визуально разделяет, у кого находится заказ (`👨‍✈️ Исполнитель (Водитель)` или `🎧 Исполнитель (Диспетчер)`).

### 2. Защита Контента (Глобальные Настройки)
**Суть:** Динамическое управление запретом на пересылку/скриншоты (`protect_content`).
**Реализация:**
- В Prisma добавлена новая таблица `BotSettings`.
- В главное меню для Главного Администратора добавлена кнопка `⚙️ Настройки`.
- Админ может в один клик менять параметр **"🛡 Защита контента: ВКЛ / ВЫКЛ"**. Это изменение моментально применяется ко всем новым заявкам, отправляемым в общую ленту водителей (сообщения самим админам никогда не защищаются для удобства управления ресурсами).

### 3. Модерация закрытого чата
**Суть:** Защита от утечек и спама в чате водителей.
**Реализация:**
- Бот выдает индивидуальные ссылки-приглашения (на 1 вход, на 24 часа) через кнопку "💬 Чат". ID закрытой группы вшит напрямую в код.
- Бот прослушивает группу и автоматически удаляет: сообщения с матом, ссылками, политическими обсуждениями, а также системные уведомления (кто-то зашел/вышел/закрепил сообщение). 

### 3. Ошибка Базы Данных в Dokploy (The column main.Order.dispatcherId does not exist)
**Причина:** При деплое монтируется внешний `volume` на папку `/app/prisma`. Из-за этого Dokploy всегда использовал старый `schema.prisma` файл, лежащий в томе, игнорируя новые поля из GitHub. `npx prisma db push` проходил вхолостую.
**Решение:**
- В `Dockerfile` добавлено создание резервной копии схемы: `COPY --from=builder /app/prisma ./prisma_backup`.
- При старте контейнеров (`Dockerfile` и `wait-for-db.sh`) бот теперь принудительно копирует новый `schema.prisma` с заменой `cp -f /app/prisma_backup/schema.prisma /app/prisma/schema.prisma`, а затем локально на сервере делает пуш в `dev.db`.

### 4. Ошибки сборки Dokploy (npm ci и No Space Left)
**Причина:** Сборка падала из-за рассинхрона `package-lock.json` и нехватки места от старых кэшей (`no space left on device`).
**Решение:** 
- В `Dockerfile` жесткая команда `npm ci` заменена на `npm install`.
- Сервер регулярно требует очистки. При возникновении `no space left`, необходимо делать **Docker Prune** (или `Prune System`) через настройки Dokploy или по SSH на сервере Beget после его жесткой перезагрузки. Очистка освобождает кэш образов (около 6-10 ГБ).

---

## ☀️ Что нужно сделать / проверить:
1. Выполнить **Hard Reboot** (Перезагрузку) VPS сервера через панель Beget.
2. Сделать `Prune System` в Dokploy (чтобы очистить память).
3. Запустить `Deploy` бота в Dokploy с новыми изменениями (1.3.0).
4. Протестировать функционал `"Взять в работу"` для Диспетчеров и кнопки `"Настройки"` для Админов.
5. Запросить генерацию 2-ого логотипа "GT" у нейросети (когда спадет нагрузка на сервера Google).
